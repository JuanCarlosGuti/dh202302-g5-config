server:
  port: 8090

spring:
  application:
    name: gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: usuarios-microservicio
          uri: lb://usuarios-microservicio
          predicates:
            - Path=/api/alumnos
        - id: api-wallet
          uri: lb://api-wallet
          predicates:
            - Path=/wallet
        - id: alumnos
          uri: lb://alumnos
          predicates:
            - Path=/alumnos


info:
  app:
    name: ${spring.application.name}

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceURL:
      defaultZone: http://localhost:8761/eureka
management:
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: gateway
